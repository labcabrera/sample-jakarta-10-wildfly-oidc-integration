apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-mcm-demo.fullname" . }}
  labels:
    {{- include "helm-mcm-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-mcm-demo.selectorLabels" . | nindent 4 }}
