spring:
  application:
    name: mcm-demo-consumer-customers-activation
  cloud:
    stream:
      bindings:
        customerCreated-in-0:
          destination: ${APP_KAFKA_TOPIC_CUSTOMER_CREATED:customers-created-topic}
          #group: customer-consumer-group
      kafka:
        binder:
          brokers: ${APP_KAFKA_BROKER:192.168.49.2:30627}
          configuration:
            security.protocol: SASL_PLAINTEXT
            sasl.mechanism: PLAIN
            sasl.jaas.config: >
              org.apache.kafka.common.security.plain.PlainLoginModule required
              username="${APP_KAFKA_USERNAME:user1}"
              password="${APP_KAFKA_PASSWORD:Ho6d2t4SeH}";
